'use strict'

const cars = [ { name: 'c1', color: 'red', speed : 40 }, { name: 'c2', color: 'blue', speed: 20 }];

if (cars[{speed}] > 20) {

    console.log(c1);
    
}

else {

    console.log(c2)
}